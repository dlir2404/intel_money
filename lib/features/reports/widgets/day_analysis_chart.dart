import 'package:flutter/material.dart';

class DayAnalysisChart extends StatelessWidget {
  const DayAnalysisChart({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
